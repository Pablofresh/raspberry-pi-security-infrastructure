# Screenshots Folder

Recommended screenshots:

- Pi-hole dashboard showing blocked queries and status
- Grafana dashboard showing Pi metrics
- PowerShell `Test-NetConnection` results for LAN tests
- PowerShell `Test-NetConnection` results for Tailscale tests
- Sanitized UFW numbered status
- Sanitized `ss -tulpn` output

Before uploading screenshots, crop or redact:

- Email addresses
- Public IPs
- Tailscale account names
- Exact private device details you do not want public
- Passwords, keys, tokens, or browser session data
